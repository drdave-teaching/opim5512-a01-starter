{
  "tests": [
    {
      "name": "PNG exists in A01-eda/figs",
      "run": "bash -lc 'set -e; if ls A01-eda/figs/*.png >/dev/null 2>&1; then echo ::notice::Found PNGs:; ls -1 A01-eda/figs/*.png; else echo ::error::No PNG found in A01-eda/figs. Save with plt.savefig(\"../figs/<name>.png\"); ls -la A01-eda/figs || true; exit 1; fi'",
      "comparison": "included",
      "timeout": 20,
      "points": 4
    },
{
      "name": "PNG was updated in this PR",
      "run": "bash -lc \"set -e; git fetch -q origin main || true; if git rev-parse --verify origin/main >/dev/null 2>&1; then CHANGED=$(git diff --name-only origin/main -- 'A01-eda/figs/*.png' || true); if [ -n \\\"$CHANGED\\\" ]; then echo ::notice::Changed PNGs:; echo \\\"$CHANGED\\\"; else echo ::error::No figure changes detected. Please update a figure in A01-eda/figs and re-commit.; git diff --name-only origin/main...HEAD | sed 's/^/Changed file: /' || true; exit 1; fi; else echo ::notice::No baseline (first push) - skipping diff check.; fi\"",
      "comparison": "included",
      "timeout": 25,
      "points": 4
    },
    {
      "name": "README note updated",
      "run": "bash -lc 'set -e; if grep -Ei \"result|figure|summary|bins|hist|png\" A01-eda/README.md; then echo ::notice::README contains a short note.; else echo ::error::Add a 1–2 sentence note to A01-eda/README.md describing your change (e.g., bins=12).; exit 1; fi'",
      "comparison": "included",
      "timeout": 20,
      "points": 2
    }
  ]
}
